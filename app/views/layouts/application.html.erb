<!DOCTYPE html>
<html>
  <head>
    <title>RealBets254 | </title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="navbar navbar-inverse navbar-fixed-top"><!--  Nav  -->
          <div class="container-fluid">
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
              <a href="/" class="navbar-brand">RealBets254</a>
            </div>
            <div class="collapse navbar-collapse" id="navbar-collapse">
              <ul class="nav navbar-nav navbar-right">
                <% if current_user %><li><%= link_to 'Betting Tips', bet_tips_path %></li><% end %>
              <li><%= link_to 'About', static_pages_about_path %></li>
              <li><%= link_to 'Contact', static_pages_contact_path %></li>
                <!-- <li>
                <% if user_signed_in? %>
                  <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
                <% else %>
                  <%= link_to('Login', new_user_session_path) %>
                <% end %>
                </li> -->
                <li class="user-login dropdown">
                  <% if user_signed_in? %>
                     
                     <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                             Logged in as: <%= current_user.first_name %><span class="caret"></span></a>
                      <ul class="dropdown-menu">
                        <li>
                            <%= link_to 'Profile', user_path(current_user.id) %>
                        </li>
                        <li>
                          <%= link_to 'Settings', edit_user_registration_path %>
                        </li>
                      </ul>       
                  <% end %>
                </li>
                
              </ul>
              
            </div>
          </div>
        </nav>
          <% if notice %>  
            <p id="notice" class="alert alert-info">
              <%= notice %>
            </p>
          <% end %>
          <% if alert %> 
            <p id="alert" class="alert alert-info">
              <%= alert %>
            </p>
          <% end %>
          
          <div class="alert alert-info alert-dismissible" role="alert">
  <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
  <strong>Beta Version!</strong> Better check yourself, you're not looking too goodThis application is running on a beta version version 1.0 is to be released soon!!!!!
</div>
</div>
          <%= yield %>
        <!-- <li>
                    <%= form_tag("/bet_tips", method: "get", class: "SearchFormSidenav" ) do  %>
                    <%= text_field_tag(:q,nil,:placeholder => "Search...", :class=>"form-control") %>
                    <!--<%= submit_tag("Go", :class =>"btn btn-outline-warning") %>-->
                  <% end %>
                </li>
  </body>
</html>